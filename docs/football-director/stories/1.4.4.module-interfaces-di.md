# Story 1.4.4: Implement Module Interfaces and Dependency Injection

## Status

Approved

## Story

**As a** developer,
**I want** clear interfaces for all engine modules with dependency injection support,
**so that** modules can be tested in isolation and dependencies are explicit.

## Acceptance Criteria

1. Interface definitions created for all modules:
   - Example: `IMatchSimulator`, `ITransferMarket`, `IFinanceEngine`, etc.
   - Interfaces define public methods and contracts
   - Stored in: `libs/football-director-engine/src/lib/interfaces/`

2. Dependency injection pattern implemented:
   - Modules accept dependencies via constructor or function parameters
   - No hardcoded dependencies between modules
   - Example: MatchSimulator accepts IPlayerStatsTracker, IInjuryManager, etc.

3. Factory functions for creating module instances:
   - `createMatchSimulator(dependencies)` - Production instance
   - `createMockMatchSimulator(overrides)` - Test instance with mocks

4. Module registration system:
   - Central registry for module instances
   - Dependency resolution at application startup
   - Easy swapping of implementations for testing

5. Clear dependency rules:
   - Engine modules depend only on other engine modules (no React/UI)
   - Zustand stores depend on engine modules (not vice versa)
   - Components depend on stores (not directly on engine)

6. Documentation on module interface patterns and DI usage

7. Example test demonstrating DI for isolated testing

## Integration Verification

- **IV1**: All engine modules work with DI pattern
- **IV2**: Unit tests use mocked dependencies successfully
- **IV3**: No circular dependencies introduced
- **IV4**: Module interfaces accurately represent contracts

## Tasks / Subtasks

- [ ] Create interfaces directory and base interface structure (AC: 1)
  - [ ] Create `libs/football-director-engine/src/lib/interfaces/` directory
  - [ ] Define base interface patterns and naming conventions
  - [ ] Create index file for clean imports

- [ ] Define interfaces for all 24+ engine modules (AC: 1)
  - [ ] Create `IMatchSimulator` interface
  - [ ] Create `ISeasonManager` interface
  - [ ] Create `ILeagueTableManager` interface
  - [ ] Create `ICupManager` interface
  - [ ] Create `ITransferMarket` interface
  - [ ] Create `IFinanceEngine` interface
  - [ ] Create `IPlayerDevelopment` interface
  - [ ] Create `IContractManager` interface
  - [ ] Create `IAIContractManager` interface
  - [ ] Create `IMoraleManager` interface
  - [ ] Create `IInjuryManager` interface
  - [ ] Create `IYouthAcademyManager` interface
  - [ ] Create `ITacticsManager` interface
  - [ ] Create `INewsEngine` interface (consolidated module from 1.4.2)
  - [ ] Create `IAchievementManager` interface
  - [ ] Create `IRecordsManager` interface
  - [ ] Create `IStaffManager` interface
  - [ ] Create `IBoardManager` interface
  - [ ] Create `IPlayerStatsTracker` interface
  - [ ] Create `IMatchCommentary` interface (separated module from 1.4.3)
  - [ ] Create `IMatchPreviewGenerator` interface
  - [ ] Create `IPostMatchGenerator` interface
  - [ ] Create `IWeatherGenerator` interface
  - [ ] Create `ITeamGenerator` interface

- [ ] Refactor modules to implement their interfaces (AC: 2)
  - [ ] Update MatchSimulator to implement IMatchSimulator
  - [ ] Update all other modules to implement their respective interfaces
  - [ ] Add dependency parameters to module constructors/functions
  - [ ] Remove hardcoded dependencies between modules
  - [ ] Ensure no circular dependencies

- [ ] Create factory functions for module instantiation (AC: 3)
  - [ ] Create production factory functions (e.g., `createMatchSimulator`)
  - [ ] Create test/mock factory functions (e.g., `createMockMatchSimulator`)
  - [ ] Document factory function usage patterns

- [ ] Implement module registration system (AC: 4)
  - [ ] Create central module registry
  - [ ] Implement dependency resolution logic
  - [ ] Add module registration at application startup
  - [ ] Provide APIs for swapping implementations in tests

- [ ] Document dependency rules and patterns (AC: 5, 6)
  - [ ] Document engine-to-engine dependency rules
  - [ ] Document store-to-engine usage patterns
  - [ ] Document component-to-store patterns
  - [ ] Create DI usage guide with examples

- [ ] Create example tests demonstrating DI (AC: 7)
  - [ ] Write example test showing isolated module testing
  - [ ] Write example test showing mock dependencies
  - [ ] Write example test showing factory usage

- [ ] Update existing tests to use DI pattern
  - [ ] Refactor existing engine module tests to use DI
  - [ ] Verify all tests pass with new DI pattern

- [ ] Verify integration requirements
  - [ ] Verify IV1: All engine modules work with DI pattern
  - [ ] Verify IV2: Unit tests use mocked dependencies successfully
  - [ ] Verify IV3: No circular dependencies introduced
  - [ ] Verify IV4: Module interfaces accurately represent contracts

## Dev Notes

### Context from Previous Stories

**From Story 1.4.2 (Consolidate News Generation):**
- Created unified `NewsEngine` module consolidating scattered news generation logic
- NewsEngine provides: `generateMatchNews`, `generateTransferNews`, `generateInjuryNews`, `generateContractNews`, `generateAchievementNews`, `generateSeasonNews`, `generateRandomNews`

**From Story 1.4.3 (Separate Match Commentary):**
- Separated `MatchCommentary` module from news generation
- MatchCommentary provides: `generateEventCommentary`, `generateHalfTimeCommentary`, `generateFullTimeCommentary`
- Clear separation: MatchCommentary = real-time match narration, NewsEngine = post-match articles

### Relevant Source Tree

**Engine Modules Location**: `libs/football-director-engine/src/lib/`

**All 24+ Engine Modules:**
1. types.ts - Core type definitions
2. match-simulator.ts - Match simulation with events
3. season-manager.ts - 52-week season system
4. league-table-manager.ts - Table updates
5. cup-manager.ts - Cup competition system
6. transfer-market.ts - Player transfers
7. finance-engine.ts - Budget, wages, prize money
8. player-development.ts - Age-based development
9. contract-manager.ts - Contract expiry, renewals
10. ai-contract-manager.ts - AI teams contract management
11. morale-manager.ts - Player morale
12. injury-manager.ts - Injury simulation
13. youth-academy-manager.ts - Youth generation
14. tactics-manager.ts - Advanced tactics
15. news-engine.ts - Unified news generation (from 1.4.2)
16. achievement-manager.ts - Achievement tracking
17. records-manager.ts - Records tracking
18. staff-manager.ts - Staff hiring/firing
19. board-manager.ts - Board objectives
20. player-stats-tracker.ts - Stats tracking
21. match-commentary.ts - Match narratives (from 1.4.3)
22. match-preview-generator.ts - Pre-match analysis
23. post-match-generator.ts - Post-match analysis
24. weather-generator.ts - Weather conditions
25. team-generator.ts - Team generation

**Current Hook Architecture** (from coding-standards.md):
- Main Hook: `src/hooks/useGameState.ts` - Orchestrates all sub-hooks
- Sub-Hooks: `useGamePersistence.ts`, `useDerivedGameState.ts`, `useGameActions.ts`, `useWeeklySimulation.ts`
- Pattern: Composable hooks architecture with single source of truth

**Key Architectural Patterns**:
- Engine Layer: Pure TypeScript, no React/UI dependencies
- State Management: Composable hooks (stores depend on engine, not vice versa)
- Module Pattern: Classes with static/instance methods, pure functions

### Dependency Injection Goals

**Why DI is Important:**
1. **Testability**: Mock dependencies in unit tests without complex setup
2. **Flexibility**: Swap implementations (e.g., use different match simulator for tests)
3. **Clarity**: Explicit dependencies make relationships clear
4. **Maintainability**: Easier to refactor when dependencies are injected

**DI Pattern Example:**
```typescript
// Before (hardcoded dependencies)
class MatchSimulator {
  simulateMatch(homeTeam, awayTeam) {
    // Directly creates instances
    const injuryManager = new InjuryManager();
    const statsTracker = new PlayerStatsTracker();
    // ...
  }
}

// After (dependency injection)
interface IMatchSimulator {
  simulateMatch(homeTeam: Team, awayTeam: Team): MatchResult;
}

class MatchSimulator implements IMatchSimulator {
  constructor(
    private injuryManager: IInjuryManager,
    private statsTracker: IPlayerStatsTracker
  ) {}

  simulateMatch(homeTeam: Team, awayTeam: Team): MatchResult {
    // Use injected dependencies
    this.injuryManager.processInjuries(...);
    this.statsTracker.recordStats(...);
    // ...
  }
}

// Factory for production
export function createMatchSimulator(): IMatchSimulator {
  return new MatchSimulator(
    createInjuryManager(),
    createPlayerStatsTracker()
  );
}

// Factory for testing
export function createMockMatchSimulator(overrides?: Partial<{
  injuryManager: IInjuryManager;
  statsTracker: IPlayerStatsTracker;
}>): IMatchSimulator {
  return new MatchSimulator(
    overrides?.injuryManager ?? createMockInjuryManager(),
    overrides?.statsTracker ?? createMockPlayerStatsTracker()
  );
}
```

### Testing

**Test Framework**: Vitest (configured in Epic 1.1)

**Test File Location**:
- Engine module tests: `libs/football-director-engine/src/lib/__tests__/`
- Integration tests: `apps/football-director/src/__tests__/`

**Test Standards**:
- Follow Arrange-Act-Assert pattern
- Use mock data factories for consistent test data
- All tests must be deterministic (no random failures)
- Tests should use DI to inject mock dependencies
- Each module test should test in isolation using mocked dependencies

**Testing Requirements for This Story**:
1. Create example tests showing DI pattern usage
2. Update existing engine module tests to use DI where applicable
3. Ensure all tests pass after DI refactoring
4. Verify no circular dependencies in dependency graph
5. Test that mock factories work correctly for all modules

**Coverage Goals**: Maintain >80% unit test coverage for engine modules (from Epic 1.1)

## Change Log

| Date | Version | Description | Author |
|------|---------|-------------|--------|
| 2026-01-03 | 1.0 | Initial story created from PRD | James (Dev Agent) |

## Dev Agent Record

### Agent Model Used

Claude Sonnet 4.5 (claude-sonnet-4-5-20250929)

### Debug Log References

None

### Completion Notes List

**In Progress** - Interface Extraction Phase Completed

**Completed Work:**
1. ✅ Created `libs/football-director-engine/src/lib/interfaces/` directory
2. ✅ Created comprehensive README.md documenting DI patterns and usage
3. ✅ Created 23 interface files for all engine modules
4. ✅ Created index.ts barrel file for clean imports
5. ✅ Moved supporting types to types.ts (DevelopmentReport, MoraleInfo, MoraleLevel, HalfTimeState, FormationRequirements)
6. ✅ Refactored 6 modules to import from new interface files: weather-generator, match-commentary, morale-manager, player-development, tactics-manager, injury-manager
7. ✅ Fixed injury-manager interface signatures to match implementation
8. ✅ Build succeeds with only minor unused variable warnings

**Next Steps:**
- Refactor remaining ~18 modules to import interfaces from new location
- Add dependency injection to module constructors
- Create factory functions for all modules
- Implement module registration system
- Create example tests demonstrating DI

### File List

**Created:**
- libs/football-director-engine/src/lib/interfaces/README.md
- libs/football-director-engine/src/lib/interfaces/index.ts
- libs/football-director-engine/src/lib/interfaces/weather-generator.interface.ts
- libs/football-director-engine/src/lib/interfaces/team-generator.interface.ts
- libs/football-director-engine/src/lib/interfaces/youth-academy-manager.interface.ts
- libs/football-director-engine/src/lib/interfaces/transfer-market.interface.ts
- libs/football-director-engine/src/lib/interfaces/season-manager.interface.ts
- libs/football-director-engine/src/lib/interfaces/league-table-manager.interface.ts
- libs/football-director-engine/src/lib/interfaces/cup-manager.interface.ts
- libs/football-director-engine/src/lib/interfaces/finance-engine.interface.ts
- libs/football-director-engine/src/lib/interfaces/player-development.interface.ts
- libs/football-director-engine/src/lib/interfaces/contract-manager.interface.ts
- libs/football-director-engine/src/lib/interfaces/ai-contract-manager.interface.ts
- libs/football-director-engine/src/lib/interfaces/morale-manager.interface.ts
- libs/football-director-engine/src/lib/interfaces/injury-manager.interface.ts
- libs/football-director-engine/src/lib/interfaces/tactics-manager.interface.ts
- libs/football-director-engine/src/lib/interfaces/player-stats-tracker.interface.ts
- libs/football-director-engine/src/lib/interfaces/records-manager.interface.ts
- libs/football-director-engine/src/lib/interfaces/staff-manager.interface.ts
- libs/football-director-engine/src/lib/interfaces/board-manager.interface.ts
- libs/football-director-engine/src/lib/interfaces/achievement-manager.interface.ts
- libs/football-director-engine/src/lib/interfaces/news-engine.interface.ts
- libs/football-director-engine/src/lib/interfaces/match-commentary.interface.ts
- libs/football-director-engine/src/lib/interfaces/match-story-generator.interface.ts
- libs/football-director-engine/src/lib/interfaces/match-simulator.interface.ts

**Modified:**
- libs/football-director-engine/src/lib/types.ts (added supporting types)
- libs/football-director-engine/src/lib/weather-generator.ts (imports interface from new location)
- libs/football-director-engine/src/lib/match-commentary.ts (imports interface from new location)
- libs/football-director-engine/src/lib/morale-manager.ts (imports interface from new location)
- libs/football-director-engine/src/lib/player-development.ts (imports interface from new location)
- libs/football-director-engine/src/lib/tactics-manager.ts (imports interface from new location)
- libs/football-director-engine/src/lib/injury-manager.ts (imports interface from new location, fixed signatures)

## QA Results

(To be populated after QA review)
