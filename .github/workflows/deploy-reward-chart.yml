name: Deploy Reward Chart to Vercel

on:
  workflow_run:
    workflows: ["CI"]
    types:
      - completed
    branches:
      - main

env:
  VERCEL_ORG_ID: ${{ secrets.VERCEL_ORG_ID }}
  VERCEL_PROJECT_ID: ${{ secrets.VERCEL_PROJECT_ID_REWARD_CHART }}

jobs:
  check-changes:
    runs-on: ubuntu-latest
    if: github.event.workflow_run.conclusion == 'success'
    outputs:
      should-deploy: ${{ steps.filter.outputs.reward-chart }}
    steps:
      - uses: actions/checkout@v4
      - uses: dorny/paths-filter@v3
        id: filter
        with:
          filters: |
            reward-chart:
              - 'apps/reward-chart/**'
              - 'package.json'
              - 'package-lock.json'
              - 'nx.json'

  deploy:
    needs: check-changes
    if: needs.check-changes.outputs.should-deploy == 'true'
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - name: Install Dependencies
        run: npm ci

      - name: Build with Nx
        run: npx nx build reward-chart --configuration=production

      - name: Install Vercel CLI
        run: npm install --global vercel@latest

      - name: Create Vercel SPA Configuration
        run: |
          cat > dist/apps/reward-chart/browser/vercel.json << 'EOF'
          {
            "version": 2,
            "rewrites": [
              { "source": "/(.*)", "destination": "/index.html" }
            ]
          }
          EOF

      - name: Deploy to Vercel
        run: |
          vercel deploy dist/apps/reward-chart/browser \
            --prod \
            --token=${{ secrets.VERCEL_TOKEN }} \
            --yes \
            --confirm
        env:
          VERCEL_ORG_ID: ${{ secrets.VERCEL_ORG_ID }}
          VERCEL_PROJECT_ID: ${{ secrets.VERCEL_PROJECT_ID_REWARD_CHART }}
